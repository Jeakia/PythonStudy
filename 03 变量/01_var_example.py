#var_example (variable--变量)

a = 1
#定义了一个变量，变量三要素--变量的名称 a
# 1赋给了变量 a ; 1是int类型（整数类型）

b = 2
#定义了一个变量，变量的名称是b，变量的值是2,2是int类型

b = 8.8
#我把变量b的值改成了8，变量b的类型没有改变，还是int类型

print("a的值是 = ",  a , "a的类型是：",type(a))
print("b的值是 = ",  b , "b的类型是：",type(b))

#计算机内存
#计算机内存是暂时存放在cpu的运算数据，是外存与CPU进行沟通的桥梁，计算机中所有程序的运行都在内存中进行
#先要定义变量，才能使用变量，不然会not defined
#print(c)

c=30
print(c)
